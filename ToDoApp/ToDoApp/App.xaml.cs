using System;
using Xamarin.Forms;
using Xamarin.Forms.Xaml;

namespace ToDoApp
{
    public partial class App : Application
    {
        // note static
        public static string FolderPath { get; set; }
        public App()
        {
            InitializeComponent();

            // FolderPath stuff

            MainPage = new MainPage();
            // Don't construct this

            // instead, navigaate to instead
            // Dont' actually need to use navigate, coul djust make new
            // but navigate gives good moving feeling


            MainPage = new Navigation


                // Kal..thorugh the career
                // Knowing about
                // Showing keyborad on bottom is sooo hard
                // From ios, android native development
                // compile to native control

            // Many months, a year or more
            // Down to a day


            // When click on it a color election (touch feedback)
            // Go build a todo app, customize this
            // Do in Xamarin, then in react native
            // First make sure good at first language you learned

            // Stretch learning capabilities


            // Don't need to Dispose because Xamarin is doing it
            // Happens for you in PopAsync

        }

        protected override void OnStart()
        {
        }

        protected override void OnSleep()
        {
        }

        protected override void OnResume()
        {
        }
    }
}
